services:
  aws_jenkins:
    build:
      context: ./jenkins
      dockerfile: Dockerfile.aws.jenkins.prod
    container_name: aws_jenkins
    restart: unless-stopped
    ports:
      - "9000:9000"
    networks:
      - net_installation

networks:
  net_installation:
    driver: bridge